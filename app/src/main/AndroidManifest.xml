<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SonidoAnimal">

        <!-- Activity principal que se abre al iniciar -->
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- JuegoAnimalesActivity solo para navegar desde el menú -->
        <activity
            android:name=".JuegoAnimalesActivity"
            android:exported="true" />

        <!-- LoginActivity -->
        <activity android:name=".LoginActivity" />

        <!-- Pantallas de resultados -->
        <activity
            android:name=".ResultadoCorrectoActivity"
            android:exported="true" />

        <activity
            android:name=".ResultadoIncorrectoActivity"
            android:exported="true" />

    </application>
</manifest>
